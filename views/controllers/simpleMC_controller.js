// Define the main app module
var simpleMCApp = angular.module('simpleMCApp', []);

// Define the `TodoListController` controller
simpleMCApp.controller('simpleMCController', function($scope) {
  // $scope.users = users;
  // console.log(users);
  console.log("haha");
});
